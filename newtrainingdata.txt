You are working in the Helios repo. Complete the model rebuild and validation-prep workflow as far as possible without inventing data.

Important:
We now have a best-available single-point OpenET coordinate estimate for the likely Hamer-area Mickelsen/Mickelson field cluster.

Use this coordinate unless the repo/workbook contains a more exact field coordinate or polygon:

latitude: 44.007
longitude: -112.239

Treat this as a cluster estimate, not confirmed exact field geometry.

Reasoning:
- Workbook references include MickelsenFarms, Hamer, Market Lake Tail End, Camas, Lusk, and multiple Hamer-area field names.
- The closest strong geospatial anchor is the USBR Hamer / INL AgriMet station near 44.007417, -112.238833.
- The estimate aligns with Hamer-area farm/packing references north of Hamer and near Market Lake/Camas.
- Safer broad bounding area is approximately:
  - latitude: 43.93 to 44.02
  - longitude: -112.25 to -112.15

Do not claim this is exact field-level ground truth. Record it in metadata as an estimated cluster coordinate if metadata supports notes/provenance.

The stale deployed artifact hash is:

3b4f696011da

Goal:
Rebuild the Helios training artifacts using the real Mickelsen/Mickelson workbook and real OpenET monthly data, then deploy and protect the API from stale feature-schema mismatches.

Hard rules:
- Do not fake data.
- Do not synthesize OpenET values.
- Do not weaken tests to pass.
- Do not commit API keys.
- Do not commit .env files.
- Do not commit private raw data unless the repo already intentionally tracks it.
- Use the estimated coordinate only for OpenET extraction unless a better coordinate/polygon is found.
- If a better coordinate/polygon is found in the workbook/repo, use that instead and explain why.
- If OpenET API access is unavailable, stop and tell me exactly what is missing.

Available workbook:
The real workbook exists locally at:

/mnt/data/Water usage 2024.xlsx

Copy it into the repo as:

data/Water_usage_2024.xlsx

Required OpenET output:

data/openet_sample.csv

Required OpenET columns:

date
openet_et_mm

Coordinate to use for OpenET point request:

latitude = 44.007
longitude = -112.239

OpenET date range:
Use monthly 2024 data unless the workbook clearly requires a different range.

Part A: Prepare training inputs

1. Ensure the repo has a data directory.
2. Copy:

/mnt/data/Water usage 2024.xlsx

to:

data/Water_usage_2024.xlsx

3. Search the workbook and repo for any more exact location information:
   - latitude
   - longitude
   - field boundary
   - shapefile
   - GeoJSON
   - Hamer
   - Mickelsen
   - Mickelson
   - Market Lake
   - Camas
   - Lusk
   - sensor
   - probe
   - field name

4. If an exact field coordinate or polygon exists, prefer it over the estimate.
5. If no better location is found, use:
   - latitude 44.007
   - longitude -112.239

6. Check whether data/openet_sample.csv already exists.
7. If it exists, verify it is real OpenET monthly data and has:
   - date
   - openet_et_mm

8. If it does not exist, use the OpenET API to generate it.

OpenET request requirements:
- Use monthly data.
- Use ET.
- Use units: mm.
- Prefer Ensemble model if supported.
- Prefer gridMET reference ET if required/supported.
- Use point request at:
  - longitude -112.239
  - latitude 44.007
- Date range:
  - start: 2024-01-01
  - end: 2024-12-31
- Write output to:
  data/openet_sample.csv

9. If the repo does not already have an OpenET fetch script, create one at:

helios/scripts/fetch_openet_monthly.py

Script requirements:
- Read API key from OPENET_API_KEY.
- Never hardcode the API key.
- Accept CLI arguments:
  --start-date
  --end-date
  --longitude
  --latitude
  --output
- Call the OpenET API.
- Write a CSV with at least:
  date
  openet_et_mm
- Preserve useful raw OpenET fields if available.
- Fail clearly if:
  - OPENET_API_KEY is missing
  - API request fails
  - response format is unexpected
  - no rows are returned

10. Generate the CSV with something equivalent to:

python3 -m helios.scripts.fetch_openet_monthly \
  --start-date 2024-01-01 \
  --end-date 2024-12-31 \
  --longitude -112.239 \
  --latitude 44.007 \
  --output data/openet_sample.csv

11. Validate the CSV:
- file exists
- nonzero rows
- has date column
- has openet_et_mm column
- openet_et_mm is numeric
- dates cover monthly 2024 data
- values are plausible monthly ET values in millimeters

Do not proceed unless both files exist:

data/Water_usage_2024.xlsx
data/openet_sample.csv

Part B: Rebuild training bundle

Run:

python3 -m helios.scripts.rebuild_training_bundle \
  --mickelson-workbook data/Water_usage_2024.xlsx \
  --openet-csv data/openet_sample.csv

If it fails:
- Diagnose traceback.
- Fix legitimate code or parsing issues.
- Do not change the required input contract unless truly necessary.
- Do not invent data.
- Re-run until it succeeds or clearly report the blocker.

Part C: Verify regenerated model

Run:

python3 -m pytest

Then run:

python3 - <<'PY'
import json
from pathlib import Path

m = json.loads(Path("artifacts/model_metadata.json").read_text())

print("training_rows:", m["training_rows"])
print("cv_rmse_mean:", m["cv_rmse_mean"])
print("feature_columns:", m["feature_columns"])
print("model_hash:", m["model_hash"])

if m["model_hash"] == "3b4f696011da":
    raise SystemExit("ERROR: model_hash is still the stale old hash.")
PY

Verify:
- pytest passes
- training_rows is nonzero and plausible
- cv_rmse_mean exists and is plausible
- feature_columns exists
- model_hash changed from 3b4f696011da

If tests fail:
- Fix real failures.
- Do not weaken tests just to get green.
- Report any failures that require manual decisions.

Part D: Commit regenerated artifacts

Inspect git status.

Commit at least:
- artifacts/moisture_model.pkl
- artifacts/model_metadata.json

Also commit regenerated training CSVs only if they are intended to be tracked and do not expose private raw data.

Do not commit:
- API keys
- .env files
- private raw workbook unless intentionally tracked
- private raw coordinates if the repo should not expose them

Suggested commit message:

Rebuild moisture model with real Mickelsen and OpenET data

Push the commit.

If push auth is unavailable, stop and tell me the exact command I need to run.

Part E: Confirm Railway deploys the new artifact

After pushing, run:

curl -s https://glorious-success-production.up.railway.app/version

Pretty-print if useful:

curl -s https://glorious-success-production.up.railway.app/version | python3 -m json.tool

Confirm:
- model_artifact_hash exists
- model_artifact_hash is not 3b4f696011da
- deployed artifact corresponds to the regenerated local artifact

If Railway still reports 3b4f696011da, diagnose:
- Were artifacts committed?
- Is Railway deploying from the correct branch?
- Is the deployed service pinned to an old commit?
- Is /version reading the same metadata used by inference?
- Is the app loading artifacts/model_metadata.json from the repo?
- Is Docker/build cache preserving old artifacts?

Fix if possible. Otherwise tell me the exact Railway/GitHub setting I need to change.

Part F: Add hard schema gate only after deployed artifact updates

Only do this after Railway confirms the deployed model hash changed.

Add startup validation so the API fails if model metadata feature columns do not match the current feature builder.

Requirements:
- Load artifacts/model_metadata.json at startup.
- Extract metadata["feature_columns"].
- Get the feature columns expected/generated by the current feature builder.
- Compare names and order exactly unless the inference pipeline explicitly treats order as irrelevant.
- If mismatch, raise a clear RuntimeError showing:
  - metadata feature columns
  - feature builder feature columns
  - missing columns
  - extra columns
- Add tests proving:
  1. startup passes when columns match
  2. startup fails when columns mismatch
  3. error message identifies the mismatch clearly

Run:

python3 -m pytest

Commit and push with:

Add startup schema validation for model features

Part G: Generate validation manifest

Run:

HELIOS_VALIDATION_MODE=1 python3 -m helios.scripts.validation_preflight \
  --output artifacts/kimberly-validation-manifest.json

Verify:

ls -lh artifacts/kimberly-validation-manifest.json
python3 -m json.tool artifacts/kimberly-validation-manifest.json

Commit it if intended to be tracked and secret-free:

git add artifacts/kimberly-validation-manifest.json
git commit -m "Add Kimberly validation preflight manifest"
git push

Part H: Prepare validation scoring, but do not fake results

If field validation data exists, score it.

If field validation data does not exist, create or update the scoring script/interface and clearly report:

CANNOT SCORE: missing field validation data

Scoring rule:
Use one fixed rule only. Prefer this order:
1. designated reference probe/depth if specified by researcher
2. driest sensor
3. mean VWC

Metrics:
- MAE by horizon
- bias by horizon
- out-of-range predictions
- water/wait agreement with researcher judgment

Validation thresholds:
- MAE <= 0.03 VWC
- absolute bias <= 0.02 VWC
- no invalid VWC predictions
- directionally reasonable irrigation decisions

Output one final status:
- VALIDATED
- PROMISING BUT NOT VALIDATED
- NOT VALIDATED
- CANNOT SCORE: missing field validation data

Do not claim validation is complete unless real field validation data was scored.

Part I: Final report

At the end, give me:

1. What was completed
2. What could not be completed
3. Exact blockers
4. Coordinate used for OpenET
5. Whether the coordinate was exact or estimated
6. Current local model hash
7. Current Railway model hash
8. Whether old hash 3b4f696011da is gone
9. Files changed
10. Commits created
11. Commands I still need to run manually
